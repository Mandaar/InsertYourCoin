# SQA — Assurance qualite logicielle (InsertYourCoin)

> Systeme leger **inspire de la QA de REGNUM AETERNUM**, adapte a un outil solo Python.
> But : tracer les bugs, **correler chaque correctif a un test de non-regression**, et
> **reutiliser le vecu** (on s'inspire des enquetes passees avant d'en lancer une nouvelle).
> Enquetes narratives detaillees : `docs/ENQUETE_ET_AMELIORATIONS.md`.

## 1. Severites (adaptees au trading)
- **P0 — Critique** : risque financier reel (ordre live errone, perte de capital), crash en
  live, garde-fou de securite contourne (`VERIFY_SSL`, plafonds `config.py`, secret commite).
  -> on **arrete tout** jusqu'a resolution.
- **P1 — Majeur** : fonction cassee (connexion Kraken KO, collecte interrompue, calcul
  PnL/risque faux) ou **diagnostic impossible** (pas de logs).
- **P2 — Mineur** : degradation non bloquante (affichage, log bruyant, edge case rare).
- **P3 — Cosmetique** : confort, formatage, libelles.

## 2. Cycle de vie d'un bug
`Ouvert -> En enquete -> Corrige (commit) -> Verifie -> Ferme`
- **Ouvert** : symptome consigne dans le registre (§5, statut Ouvert).
- **En enquete** : appliquer la doctrine (`ENQUETE_ET_AMELIORATIONS.md` §1). **Etape 0
  obligatoire : s'inspirer du vecu** (cf. §3).
- **Corrige** : cause racine + **garde-fou par construction** ; commit reference dans le registre.
- **Verifie** : un **test de non-regression** existe et passe, + comportement observe
  (logs / dashboard / `stats`).
- **Ferme** : registre a jour (severite, commit, test, lien enquete).

> **Regle d'or (Loi anti-recidive)** : un bug n'est *ferme* que s'il a un **test** qui
> empeche sa recurrence. **Pas de test = bug encore ouvert.**

## 3. La systemique "s'inspirer du vecu" (etape 0 de toute enquete)
Avant toute nouvelle enquete, **lire d'abord** : le **registre des bugs** (§5) et le
**journal d'enquetes** (`ENQUETE_ET_AMELIORATIONS.md` §2). Beaucoup de symptomes se
repetent (reseau, SSL, frais, signal). Reutiliser une cause / un correctif deja documentes
fait gagner des heures et evite de re-deriver depuis zero.
- Cas **deja vecu** -> appliquer / adapter le correctif connu, verifier qu'il tient.
- Cas **nouveau** -> enqueter, puis **capitaliser** : nouvelle ligne au registre (§5) +
  enquete detaillee (§2 du carnet).

## 4. Gate qualite (avant un merge significatif ou un passage en live)
- [ ] `pytest` tout vert (sans reseau).
- [ ] Aucun **P0 / P1** ouvert.
- [ ] Garde-fous live intacts (dry-run par defaut, plafonds `config.py`, double confirmation).
- [ ] Aucun secret commite (`git status` : pas de `.env`).
- [ ] `VERIFY_SSL = True`.
- [ ] Chaque bug corrige du lot a son **test de non-regression**.

## 5. Registre des bugs (correlation bug <-> correctif <-> test)

| ID | Date | Sev | Symptome | Cause racine | Correctif (commit) | Test non-regression | Enquete | Statut |
|----|------|-----|----------|--------------|--------------------|---------------------|---------|--------|
| BUG-001 | 2026-06-05 | P1 | `CERTIFICATE_VERIFY_FAILED` contre Kraken | Avast intercepte le HTTPS ; sa CA racine est absente du bundle `certifi` | `9c17bac` puis centralise `5816567` (`truststore` -> magasin de certificats de l'OS ; `VERIFY_SSL=True`) | `tests/test_healthcheck.py` (`diagnose_error` -> ssl) | carnet §2 (#0) | **Ferme** |
| BUG-002 | 2026-06-05 | P1 | `kraken GET .../OHLC` en boucle la nuit + collecte morte ; process arrete | Timeouts reseau nocturnes **non journalises** (aucun log paper) + mise en veille du PC | `b9cf6d0` (logs paper typuees, backoff, timeout 30s, detection refus Kraken/DDoSProtection) + `42ae373` (dashboard) | `tests/test_resilience.py` | carnet §2 (#1) | **Ferme** |
| BUG-003 | 2026-06-07 | P2 | `config.FEE=0.0026` sous-estime le taker Kraken reel -> backtests & paper FLATTES (conclusions optimistes) | Taker spot Kraken palier de base = **0.40%** (maker 0.25%) d'apres la doc officielle, pas 0.26% | `config.FEE` 0.0026 -> 0.0040 -> **0.0080** (taker palier base 0.80% des le 9/07/2026 ; on simule des ordres marche -> taker FUTUR, prudent) | `tests/test_config.py` (assert FEE>=0.008) | panel §0 (hygiene de mesure) | **Ferme** |
| BUG-004 | 2026-06-07 | P1 | `compare` / `walkforward` CRASHENT sur Windows (UnicodeEncodeError) ; le walk-forward (le JUGE) etait inutilisable | Console Windows en cp1252 : `print()` d'un caractere non-cp1252 (sigma de Bollinger, accents FR, fleches/emoji du verdict) leve l'erreur | `main.py` force stdout/stderr en **UTF-8** (`errors='replace'`) | `tests/test_config.py::test_main_force_stdout_utf8` | carnet §2 (#2) | **Ferme** |
| BUG-005 | 2026-06-08 | P1 | Walk-forward : warm-up des indicateurs perdu a chaque fenetre OOS -> classements fausses (SMA 200 amputee) ; **contamine le verdict Etape 1** | Indicateurs recalcules sur chaque tranche isolee | `Backtester.run(df, strategy, warmup=0)` calcule les signaux sur tout le df mais ne compte equity/trades/metriques qu'a partir de `warmup` (rebase au capital initial) ; `optimizer.py` backteste les fenetres OOS sur une fenetre etendue `WARMUP=250` bougies amont (non commite, cf. session 2026-06-08) | `tests/test_optimizer.py::test_warmup_revives_slow_sma_on_short_window` + `::test_warmup_rebases_equity_and_total_return` | AUDIT_MOTEUR B1 | **Verifie** |
| BUG-006 | 2026-06-08 | P1 | optimizer selectionne sur metrique non bornee (flat=0 bat les negatives ; `inf` gagne) -> biais de selection (inaction presentee "robuste") | `_best_on` argmax sans garde (`optimizer.py:45,51`) | metriques degenerees -> `NaN` (jamais 0.0 ni inf) cote backtester ; `_best_on` n'eligir qu'une combo a metrique FINIE ET `n_trades>=MIN_TRADES=5` (sinon fallback fini marque `degenerate`) ; `avg_window_metric` ignore NaN/inf (non commite) | `tests/test_optimizer.py::test_best_on_skips_flat_combo_when_a_trading_one_exists` + `::test_best_on_fallback_is_finite_and_flagged_when_no_eligible` + `::test_avg_window_metric_ignores_nan_and_inf` | AUDIT_MOTEUR B2 | **Verifie** |
| BUG-007 | 2026-06-08 | P2 | `optimizer.py` (le "juge" du projet) sans AUCUN test de non-regression | aucun `test_optimizer.py` | `tests/test_optimizer.py` cree : non-chevauchement fenetres, train precede test, garde "pas assez de donnees", reproductibilite, + couverture B1/B2 (non commite) | `tests/test_optimizer.py` (12 tests) | AUDIT_MOTEUR B3 | **Verifie** |

| BUG-008 | 2026-06-10 | P1 | Page Options inenregistrable : TOUT POST repondait 404 | Typo de route dans `do_POST` : backslash-options au lieu de `/options` (les 33 tests purs ne couvraient pas le handler HTTP) | Route corrigee + `build_monitor_server` extrait (serveur testable port ephemere) | `tests/test_monitor_server.py` (5 tests d'INTEGRATION : vraies requetes loopback, POST CSRF de bout en bout) | trouve en review orchestrateur AVANT commit | **Ferme** |
| BUG-009 | 2026-07-10 | P2 | `lancer.py --status` affiche « paper : EN COURS (PID 15844) » a tort (paper mort depuis 6,5 h) ; `_start_service` REFUSE alors de demarrer un nouveau paper (« deja en cours ») -> double-clic sur l'icone bloque | Recyclage de PID Windows : `run/paper.pid` rance pointe un PID recycle vers `svchost.exe`. `do_status`, `_start_service` et le resume de `do_start` testaient `pid_alive(pid)` (EXISTENCE) au lieu de l'IDENTITE. `is_our_process(pid, service, start_ts)` (cmdline main.py+service + create_time) existait deja mais n'etait utilisee que par `do_stop` | branche `fix/pid-recycling-identity-check` : les 3 points de decision passent a `is_our_process(pid, name, read_pid_start(pid_path))` ; un PID vivant-mais-recycle est traite comme ARRETE et le pid file rance est nettoye (comme `do_stop`) | `tests/test_launcher.py::test_do_status_reports_stopped_when_pid_recycled` + `::test_start_service_starts_when_pid_recycled` + `::test_do_status_reports_running_when_identity_confirmed` (+ maj `::test_start_service_does_not_double_a_running_service`) | signale par l'utilisateur (constat 2026-07-10) | **Verifie** |

| BUG-010 | 2026-07-16 | P1 | **Walk-forward web, mono-actif : bandeau VERT « EDGE PLAUSIBLE » la ou la CLI equivalente dit « indecidable » ou « sur-apprentissage probable »** -> l'ecran le plus visible de l'app survend un resultat que la CLI juge severement (violation directe du garde-fou n1 : honnetete) | `research_runners.py` route TOUJOURS par `walk_forward_multi`, meme pour 1 symbole. `optimizer.walk_forward_multi` : `robust = (n_positive*2 > n_assets) AND (avg_oos > 0)` degenere pour `n_assets=1` en `oos_total_return > 0`. `walkforward_page._verdict_banner` : `robust=True` -> VERT ; l'orange exige `robust=False ET n_positive>0`, inatteignable pour n=1 -> mono est strictement vert-ou-rouge. Les 2 branches severes de la CLI (`optimizer._verdict` : NaN -> « indecidable », metrique < 0.5 -> « sur-apprentissage probable ») n'existent NULLE PART cote web (`avg_window_metric` absent de tous les `*_page.py`) | `_mono_severity()` dans `walkforward_page.py` : rejoue les 4 branches de `optimizer._verdict(avg_window_metric, 1.0, oos, wf=True)` (INDECIDABLE / NE PAS TRADER / SUR-APPRENTISSAGE PROBABLE / EDGE PLAUSIBLE) ; `_verdict_banner` l'utilise pour n_assets==1 au lieu de `robust` degenere. Multi-actifs inchange. Aucun recalcul moteur (lit `avg_window_metric` deja present) | `tests/test_walkforward_page.py` : parite STRICTE par import direct de `optimizer._verdict` rejoue sur les memes entrees + branches NaN/negatif/<0.5 testees une a une | revue adversariale Lot 6 (lentille honnetete), finding confirme apres refutation | **Verifie** |
| BUG-011 | 2026-07-16 | P2 | **Walk-forward web : un holdout qui ECHOUE ne module ni ne qualifie le bandeau vert affiche au-dessus**, et le verdict CLI du holdout (« Ne pas trader » / « indecidable ») est perdu | Le bandeau lit uniquement `summary["robust"]` calcule sur le segment RECHERCHE (le holdout est retire avant l'appel et stocke a part, jamais lu par le bandeau). `optimizer.format_holdout` termine par `_verdict(...)` -> « Ne pas trader » si negatif ; la carte web `_holdout_block` n'affiche que des chiffres bruts, sans equivalent de `_verdict`. Resultat : « VERDICT : EDGE PLAUSIBLE » (non qualifie) peut surplomber un holdout qui dit l'inverse | `_holdout_severity()` (rejoue `optimizer._verdict(metric, 0.0, total_return)` = meme appel que `format_holdout`) + badge 'Verdict validation finale' par symbole dans `_holdout_block` ; `_worst_holdout_verdict` DEGRADE le bandeau (jamais ne l'ameliore) avec libelle qualifie '<verdict> -- VALIDATION FINALE : <verdict holdout>' | `tests/test_walkforward_page.py` : parite directe `_holdout_severity` vs `optimizer._verdict` + test 'holdout negatif -> bandeau degrade + verdict rendu' + test 'ne jamais ameliorer' | revue adversariale Lot 6 (lentille honnetete), finding confirme apres refutation | **Verifie** |

| BUG-012 | 2026-07-16 | P2 | **Test FLAKY** : `tests/test_monitor_server.py::test_route_static_sert_chart_js_vendorise` echoue par intermittence (~10 %, mesure sur 40 runs isoles), meme en isolation totale -> **la gate SQA devient non fiable** (un vert/rouge aleatoire ne prouve rien) | **DEUX causes distinctes, chacune mesuree** (2026-08-08) : **(A) fuite de socket reelle dans les fixtures de test** -- `srv.shutdown()` seul ne ferme JAMAIS le socket d'ecoute (`fileno()` reste valide) car `server.RequestHandlerClass` (Handler) ferme un CYCLE de references sur `srv` via les closures des routes `/server/stop`/`/server/restart` (`server_ref[0] = srv`) : le refcounting seul ne libere jamais l'objet, prouve par script (gc desactive : non collecte ; `gc.collect()` : 96 objets cycliques retrouves). Corrigee (hygiene reelle, meme pattern deja utilise par `_restart_server_thread` en prod, `trading/monitor.py:804`) mais **INSUFFISANTE SEULE** : le flake a persiste a l'identique (1/10) sur une suite CLEAN, sequentielle, sans aucune pollution possible (test unique en isolation totale : 4/40 echecs). **(B) cause racine reelle : interference reseau EXTERNE non-deterministe** -- un antivirus actif sur la machine (Avast : `AvastSvc` + 4x `AvastUI` confirmes en cours d'execution, `Get-Process`) intercepte le trafic HTTP en boucle locale. `/static/chart.umd.min.js` est le SEUL fichier reellement lu sur disque et transmis tel quel (205 Ko, `content-type: application/javascript`) de toute la suite -- toutes les autres routes generent du HTML en memoire -- et concentre l'essentiel des occurrences. Mesures : 40 runs isoles du seul test -> 4 timeouts client a un delai QUASI CONSTANT ~5,9 s (signature d'un budget interne d'un outil externe, pas un GC pause aleatoire) ; sonde dediee timeout=20s -> 1/25 `ConnectionResetError` (WinError 10054 "connexion fermee par l'hote distant") apres ~19 s = connexion ACTIVEMENT COUPEE cote pair (aucun allongement de timeout client ne peut garantir la reussite dans ce cas precis) ; **1 occurrence collaterale observee sur une AUTRE route** (`test_post_route_inconnue_404`, suite complete run 3/3) prouvant que l'interference n'est pas specifique au fichier JS mais touche potentiellement toute requete HTTP loopback (taux residuel tres faible, hors-scope de ce correctif) | **(A)** `tests/test_monitor_server.py` : helper `_teardown_server()` (`type(srv).shutdown(srv)` + `type(srv).server_close(srv)`, methodes non liees pour rester correctes meme si un test monkeypatche `shutdown`/`server_close` sur l'instance), utilise par les 3 fixtures `server`/`server_obj`/`server_with_jobs` ; meme correctif dans `tests/test_launcher.py::test_monitor_signature_present_matches_real_root_page`. **(B)** `test_route_static_sert_chart_js_vendorise` : retry borne a 2 tentatives (`TimeoutError`, `ConnectionResetError`, `urllib.error.URLError`), **AUCUN affaiblissement d'assertion** (memes 2 assertions qu'avant : `code == 200` et `"Chart.js" in page`), scope limite a CE test (le helper partage `_get()` reste inchange). Aucun fichier `trading/` touche (cause racine externe, hors code applicatif) | `tests/test_monitor_server.py::test_teardown_server_ferme_reellement_le_socket_decoute` + `::test_teardown_server_ferme_le_socket_meme_si_shutdown_est_mocke_sur_linstance` (non-regression fuite socket, preuve directe `fileno()==-1`) ; le retry est couvert par re-execution du test lui-meme (40/40 passes post-correctif, dont 6/40 avec 1re tentative avortee par l'AV et sauvee par la 2e) | investigation qa-tester 2026-08-08 : reproduit 3x independamment (boucle fichier isole, boucle suite complete, sonde dediee) avant correctif ; **stabilite post-correctif** : 40/40 (test seul) + 10/10 (fichier, boucle) + 3/3 (suite complete) -- disponible integralement dans le scratchpad de session | **Verifie** |

| BUG-013 | 2026-08-08 | P2 | `lancer.py do_start` ne reconnaissait plus jamais un monitor deja lance (signature introuvable) -> navigateur jamais ouvert sur serveur existant, silencieusement | `monitor_signature_present()` cherchait "Paper trading - monitoring" sur `/` -- route devenue l'Accueil a la bascule du Lot 1 ; la signature ne vit plus que sur `/monitoring` | Signature remplacee par "Local 127.0.0.1" (nav commune a TOUS les ecrans, robuste aux bascules de route) -- trouve par l'agent tools-programmer en TESTANT reellement (V4) | `tests/test_launcher.py` (signature sur page reelle) | chantier zero-fenetre 2026-08-08 | **Verifie** |
| BUG-014 | 2026-08-08 | P1 | Bouton « Redemarrer le serveur » : l'ancien serveur s'arrete mais le NOUVEAU ne nait jamais -> l'app meurt (pire que pas de bouton). Aucune trace nulle part | TRIPLE cause, chacune mesuree : (1) thread de service `daemon=True` tue a la fin de serve_forever() avant le Popen ; (2) cause dominante : `open(monitor_console.log,'ab')` leve PermissionError car la redirection shell '>>' du monitor courant tient ce fichier en verrou EXCLUSIF ; (3) `except OSError: pass` AVALE l'echec en silence (violation M9) | `_service_thread()` non-daemon PAR CONSTRUCTION + log DEDIE `monitor_respawn.log` (aucun autre detenteur) + repli DEVNULL si verrou (le respawn prime sur son log) + echec ecrit dans `monitor_respawn_error.log` (jamais silencieux) + `-u` sur le respawn (flush) | `tests/test_monitor_server.py::test_service_thread_est_non_daemon` + `::test_routes_stop_et_restart_passent_par_service_thread` + `::test_spawn_detached_monitor_survit_a_un_log_verrouille` + `::test_restart_thread_ecrit_l_erreur_au_lieu_de_l_avaler` + E2E reel orchestrateur (restart -> port revenu, nouveau PID) | E2E orchestrateur (V4) -- les tests unitaires mockaient le spawn et ne pouvaient PAS le voir | **Verifie** |

| BUG-015 | 2026-08-08 | P0 | Ecran Live : race TOCTOU -> DEUX process `live --execute` concurrents sur le MEME compte Kraken (deux armements legitimes, ex. deux onglets). Chaque process applique MAX_POSITION_VALUE_USD contre SA propre lecture du solde -> exposition agregee reelle > plafond. C'est LE mode de defaillance que l'archi 'process detache + verrou PID' devait eliminer | `_live_start_post` (monitor.py ~1399-1427) lit `live_identity()` PUIS `live_control` spawn PUIS ecrit le pid file, SANS `threading.Lock`, sur un `ThreadingHTTPServer`. Fenetre TOCTOU entre check 'aucun live' et ecriture pid. Le GIL protege le nonce mais PAS la sequence check->spawn->write. Reproduit 10/10 (PoC 2 nonces / 2 threads) | `_live_start_lock = threading.Lock()` (module-scope de `build_monitor_server`, a cote de `_arm_tokens`, `trading/monitor.py`) enveloppe TOUTE la sequence identite->consommation nonce->spawn->ecriture pid dans `_live_start_post`, pour les DEUX chemins (`dry` et `reel`) -- non committe (working tree, cf. Lot 8) | `tests/test_live_server.py::test_route_live_start_concurrence_un_seul_spawn_reel` -- 2 threads, 2 armements legitimes -> 2 nonces DISTINCTS, `spawn_live_detached` mocke avec `time.sleep(0.05)` (elargit la fenetre), boucle 10 essais : `len(calls) == 1` a CHAQUE essai. **Determinisme prouve dans les deux sens** : test retire temporairement le verrou (`_live_start_lock` remplace par un no-op) -> echoue immediatement (`spawns reels = 2`, essai 0/10, identique aux 10/10 de la gate) ; verrou remis -> 6/6 executions completes du test (60 essais au total) toutes vertes | gate independante Lot 8 (V3) : producteur declarait 12/12 PASS, acteur independant a reproduit le P0 -- docs/audit/GATE_LOT8_LIVE.md ; correctif implemente sur brief direct de la gate (FAIL-1 §1, correctif recommande applique a la lettre) | **Verifie** |

| BUG-016 | 2026-08-08 | P2 | `_paper_post` action 'start' : MEME patron TOCTOU que BUG-015 (status['running'] lu PUIS `_start_paper_from_params` spawn+pid, sans verrou, sur ThreadingHTTPServer) -> deux paper concurrents possibles corrompant le meme paper_state.json/paper_stats.csv | Sequence check->spawn->write non serialisee (repere par le fix BUG-015). P2 et non P0 : AUCUN argent reel en jeu (paper = fictif), mais corruption de l'historique accumule possible | `_paper_start_lock = threading.Lock()` (module-scope de `build_monitor_server`, a cote de `_live_start_lock`, `trading/monitor.py`) enveloppe la sequence RE-verification identite (`_paper_identity`) -> `_start_paper_from_params` (spawn+pid) dans `_paper_post`, action "start" -- meme patron que BUG-015 (verrou couvre uniquement la fenetre de demarrage, "stop" et GET restent hors verrou) | `tests/test_monitor_server.py::test_route_paper_post_start_concurrence_un_seul_spawn` -- 2 threads, 2 POST /paper start concurrents, `_spawn_paper_detached` mocke avec `time.sleep(0.05)` (elargit la fenetre), boucle 10 essais : `len(calls) == 1` a CHAQUE essai. **Determinisme prouve dans les deux sens** : verrou temporairement neutralise (`with _paper_start_lock:` -> `if True:`) -> echoue immediatement (`spawns = 2`, essai 0/10) ; verrou remis -> vert (16/16 tests /paper, dont ce test) | repere en corrigeant BUG-015 | **Verifie** |

> **Nouveau bug** -> ajouter une ligne ici (statut *Ouvert*), puis suivre le cycle §2.
> Severite des le constat ; ne jamais fermer sans test.

---
*Inspiration : QA REGNUM (qa-lead / referentiel de regression / gate review), version solo.*
